What happens when you initialize a repository? 

.git file gets created

Why do you need to do it?
to start tracking



How is the staging area different from the working directory and the repository?

staging area is intermediate stage between pushing changes from working directory to repo

What value do you think it offers?
we can commit the files we want and ignore the files we dont want to commit.



How can you use the staging area to make sure you have one commit per logical
change?
by commiting each time one logical change we can ensure if we want to revert incase of any bugs




What are some situations when branches would be helpful in keeping your history
organized? 
when we want to work on multiple items and want to organize it well, we can create branch

How would branches help?
they help in working two different things simultaneously

How do the diagrams help you visualize the branch structure?
diagrams gives clear picture of how the branches were created and what was its parent branches.

What is the result of merging two branches together? 
we get updates from both branch into one

Why do we represent it in the diagram the way we do?
To understand the concepts


What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
Pros: we get control
Con: very rarely we loose the changes we made